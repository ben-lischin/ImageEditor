package controller;

/**
 * Represents a controller interface that can perform functions on its model.
 */
public interface IController {

  /**
   * Allows the user to interact with the program and perform functions on the model.
   */
  void edit();
}
